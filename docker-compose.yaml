services:
  database:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hrm
      POSTGRES_USER: hrm-user
      POSTGRES_PASSWORD: hrm-password
    ports:
      - "5432:5432"
    volumes:
      - hrm_data:/var/lib/postgresql/data

volumes:
  hrm_data:
