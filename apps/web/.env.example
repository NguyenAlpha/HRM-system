# Server-side URL used by Next.js Route Handlers to reach Spring Boot.
API_URL=http://localhost:8080
AUTH_COOKIE_SECURE=false

# Lifetime of the HttpOnly refresh-token cookie (30 days by default).
AUTH_REFRESH_COOKIE_MAX_AGE_SECONDS=2592000
